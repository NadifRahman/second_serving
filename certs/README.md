# TLS certificates

Put real TLS certificate files here on the server only. Do not commit issued
certificates or private keys.

Expected production filenames:

- `certs/fullchain.pem`
- `certs/privkey.pem`

Example placeholders are in `certs/example/`.
